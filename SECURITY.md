# Security & Privacy
- No PII processed. Only public announcements and metadata.
- Evidence bundles are hashed (SHA-256) and stored append-only.
- All uploads stay in session memory unless user downloads logs.
- No external secrets required for demo.
